基于python3.8。
okex 等交易所数据对接。采集数据，并提供数据导出。

一.结构
    coin_spider/spiders： 采集脚本，一个采集脚本，对应一个coin_db里数据库脚本。
    coin_db： 数据库脚本。除__ini__  base 外，每一个模块名对应数据库一个库名，确保该模块的数据库存在，否则程序报错。
    分表设计： 数据入库的分表设计，有的基于币对名称，有的基于时间切分。
    分库设计： 每个数据库脚本模块对应一个数据库，
    浮点数存储： 把浮点数 * 100000000 后取整，按bigint入库。
    时间存储：原utc的时间，全转换为东八时间存储。

二.运行
    1.运行数据导出服务，在根目录运行服务
      (1) python manage.py runserver 0.0.0.0:8000
      (2) 浏览器访问 http://服务的ip:8000/coin_spider/data_export   导出某时间段某币对数据。
    2.运行数据采集脚本，
      (1) 进入 coin_spider/spiders 目录，  python 运行对应的脚本即可。
      (2) 部分websocket采集脚本需要持续运行，保证数据采集不中断，一般要在两个机器运行(确保一个机器故障，数据不中断)

